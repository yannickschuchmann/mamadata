<%= form_for(@benefit_incident) do |f| %>
  <% if @benefit_incident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@benefit_incident.errors.count, "error") %> prohibited this benefit_incident from being saved:</h2>

      <ul>
      <% @benefit_incident.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id, Person.all.collect{|p| [p.name, p.id]}, include_blank: true  %>
  </div>
  <div class="field">
    <%= f.label :program %><br>
    <%= f.select :program_benefit_relationship_id, Program.all.collect{|p| [p.name, p.id]}, include_blank: true%>
  </div>
    <div class="field">
    <%= f.label :benefit %><br>
    <%= f.select :program_benefit_relationship_id, Benefit.all.collect{|b| [b.name, b.id]}, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :remark %><br>
    <%= f.text_field :remark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
