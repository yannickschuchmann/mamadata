<h2>Add a Journal Entry:</h2>
<%= form_for([@person, @person.journals.build]) do |f| %>
<div class="row">
	<div class="small-12 large-12 columns"><%= f.label "Journal Entry" %></div>
	<div class="small-12 large-12 columns"><%= f.text_field :text %></div>
</div>

<%= f.submit :class => "button" %>
<% end %>
<% @person.journals.each do |f| %>
<h5><%= f.created_at %></h5>
<p><%= f.text %></p>
<% end %>a