<%= form_for(@program) do |f| %>
  <% if @program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
      <% @program.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <div class="span3"><%= f.label :name %></div>
    <div class="span9"><%= f.text_field :name %></div>
  </div>
  <div class="field">
    <div class="span3"><%= f.label :description %></div>
    <div class="span9"><%= f.text_field :description %></div>
    </div>


<div class="row-fluid">
  <span class="ul">
 
  <% Benefit.all.each_slice(10) do |slice| %>
  <div class="span3">
  <% slice.each do |b| %>
    <label class="li"><%= check_box_tag :benefit_ids, b.id, @program.benefits.include?(b), :name => 'program[benefit_ids][]' %> <%= b.name %></label>
    <% end %>
  </div>

  <% end %>
 
  </span>
  </div>
  <br>
<div class="row-fluid">
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
  </div>
<% end %>
